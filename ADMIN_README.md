# 思维导图管理后台

这是一个用于管理思维导图思维空间和节点的Web管理界面。

## 功能特性

### 🧠 思维空间管理
- ✅ 创建新的思维空间
- ✅ 查看所有思维空间列表
- ✅ 查看思维空间详情（根节点、节点数量、创建时间）
- ✅ 删除思维空间（包括其下所有节点）
- ✅ 搜索思维空间

### 📊 节点管理
- ✅ 创建新节点
- ✅ 编辑节点信息（标题、内容）
- ✅ 删除节点
- ✅ 查看节点列表
- ✅ 按思维空间筛选节点
- ✅ 搜索节点
- ✅ 自动生成节点ID

## 技术栈

- **前端**: HTML5 + CSS3 + JavaScript (ES6+)
- **后端**: Spring Boot + PostgreSQL
- **UI框架**: 自定义CSS + Font Awesome图标
- **API通信**: Fetch API

## 快速开始

### 1. 启动后端服务

```bash
cd backend
export PATH="/opt/homebrew/opt/openjdk@17/bin:$PATH"
mvn spring-boot:run
```

后端服务将在 `http://localhost:8080` 启动

### 2. 启动管理页面

#### 方法一：使用启动脚本（推荐）
```bash
./start-admin.sh
```

#### 方法二：手动启动
```bash
cd frontend
python3 -m http.server 3000
```

然后在浏览器中访问：`http://localhost:3000/admin.html`

## 使用指南

### 创建思维空间

1. 点击"思维空间管理"标签页
2. 点击"创建思维空间"按钮
3. 输入思维空间ID（建议使用有意义的名称）
4. 点击"保存"

### 创建节点

1. 切换到"节点管理"标签页
2. 选择要添加节点的思维空间
3. 点击"创建节点"按钮
4. 填写节点信息：
   - **思维空间ID**: 自动填充
   - **父节点ID**: 留空表示根节点，或输入父节点的nodeId
   - **节点ID**: 可以手动输入或点击"生成ID"自动生成
   - **标题**: 节点显示名称
   - **内容**: 节点的详细内容（可选）
5. 点击"保存"

### 编辑节点

1. 在节点列表中找到要编辑的节点
2. 点击"编辑"按钮
3. 修改节点信息
4. 点击"保存"

### 删除操作

1. 点击要删除项目的"删除"按钮
2. 在确认对话框中点击"删除"确认

## API接口

管理页面使用以下API接口：

### 思维空间相关
- `POST /api/nodes/mind-space` - 创建思维空间
- `GET /api/nodes` - 获取所有节点（用于统计思维空间）
- `DELETE /api/nodes/mind/{mindId}` - 删除思维空间

### 节点相关
- `POST /api/nodes` - 创建节点
- `GET /api/nodes/mind/{mindId}` - 获取指定思维空间的节点
- `PUT /api/nodes/{id}` - 更新节点
- `DELETE /api/nodes/{id}` - 删除节点
- `GET /api/nodes/generate-node-id` - 生成唯一节点ID

## 界面说明

### 侧边栏导航
- **思维空间管理**: 管理所有思维空间
- **节点管理**: 管理节点（需要先选择思维空间）

### 主要功能区域
- **搜索栏**: 实时搜索功能
- **数据表格**: 显示数据列表
- **操作按钮**: 编辑、删除等操作

### 模态框
- **创建/编辑模态框**: 用于创建和编辑数据
- **确认删除模态框**: 确认删除操作

## 注意事项

1. **思维空间ID唯一性**: 每个思维空间ID必须唯一
2. **节点ID唯一性**: 在同一思维空间内，节点ID必须唯一
3. **根节点**: 每个思维空间都有一个nodeId为"root"的根节点
4. **级联删除**: 删除思维空间会删除其下所有节点
5. **父节点验证**: 创建子节点时，父节点必须存在

## 故障排除

### 后端连接失败
- 确保后端服务正在运行
- 检查端口8080是否被占用
- 查看浏览器控制台错误信息

### 数据加载失败
- 检查网络连接
- 确认后端API服务正常
- 查看浏览器开发者工具的网络标签

### 页面样式异常
- 确保所有CSS文件正确加载
- 检查浏览器兼容性
- 清除浏览器缓存

## 开发说明

### 文件结构
```
frontend/
├── admin.html          # 管理页面主文件
├── admin-styles.css    # 样式文件
├── admin-script.js     # JavaScript功能文件
└── start-admin.sh      # 启动脚本
```

### 自定义配置
- 修改 `admin-script.js` 中的 `API_BASE_URL` 来更改后端API地址
- 修改CSS变量来自定义主题颜色
- 添加新的功能模块到相应的标签页

## 更新日志

### v1.0.0 (2025-09-13)
- ✅ 初始版本发布
- ✅ 思维空间管理功能
- ✅ 节点管理功能
- ✅ 响应式设计
- ✅ 现代化UI界面
